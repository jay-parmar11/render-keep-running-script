const https=require("https"),urls=process.argv.slice(2);function makeRequest(e){https.get(e).on("error",t=>{console.error(`Error for ${e}: ${t.message}`)})}function scheduleNextRequest(e){let t=6e4*Math.floor(3*Math.random()+2);console.log(`Next request to ${e} in ${t/1e3/60} minutes.`),setTimeout(()=>{makeRequest(e),scheduleNextRequest(e)},t)}0===urls.length&&(console.error("Please provide at least one URL as a command-line argument."),process.exit(1)),urls.forEach(e=>scheduleNextRequest(e));